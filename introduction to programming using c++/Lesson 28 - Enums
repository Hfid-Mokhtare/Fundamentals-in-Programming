#include <iostream>

using namespace std;

enum EnMyFavoriteColor { blac, white, blue, green };
enum EnMaritalStatus { married=1, single=0 };
enum EnGender { male=1, femal=0 };

int main()
{



	return 0;
}

